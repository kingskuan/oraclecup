{
  "name": "oraclecup-contracts",
  "version": "1.0.0",
  "description": "OracleCup smart contracts (X Layer / Solidity 0.8.24)",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:xlayer": "hardhat run scripts/deploy.js --network xlayer",
    "deploy:xlayer-testnet": "hardhat run scripts/deploy.js --network xlayerTestnet",
    "sync-abi": "node scripts/sync-abi.js"
  },
  "devDependencies": {
    "hardhat": "^2.22.10"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "dotenv": "^16.4.5"
  }
}
