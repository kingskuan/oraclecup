{
  "name": "oraclecup-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "OracleCup dApp — single-file static site served via `serve`.",
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
